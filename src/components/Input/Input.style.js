import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    height: 40,
    backgroundColor: '#e0e0e0',
    padding: 8,
    margin: 10,
    borderRadius: 5,
    alignItems: "center",
  },
});